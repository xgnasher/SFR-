# Core
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12

# Templates (for server-rendered HTML)
jinja2==3.1.4

# Database
sqlalchemy==2.0.36

# Needed libraries
# alembic==1.13.3          # DB migrations
# psycopg2-binary==2.9.10  # Postgres driver
# pydantic-settings==2.6.0 # typed config from env vars
# httpx==0.27.2            # HTTP client (also needed for fastapi.testclient)
# pytest==8.3.3            # tests
